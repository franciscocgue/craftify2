import { Checkbox } from "@chakra-ui/react"

const CCheckbox = () => {
    return <Checkbox defaultChecked w={'100%'} h={'100%'} colorScheme='teal' size='md'>
        Checkbox
    </Checkbox>
}

export default CCheckbox;